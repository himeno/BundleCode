# Changelog

版は `上流版-bundle.N`。上流版はベースにした Code - OSS（`package.json` の `version`）、
`N` は同じベースのまま BundleCode 側の変更を出した回数。上流を取り込んだら `N` は
1 に戻る。各版はタグ `v<版>` と一致する。

## 1.132.0-bundle.1 — 2026-09-14

初回公開。Code - OSS 1.132.0 をベースに、次を加えたもの。使い方は README.md を参照。

- 複数のワークスペースを 1 つのウィンドウに束ね、左端のストリップで切り替える。
  各ワークスペースは独立した workbench と専用の拡張ホストを持ち、切り替えで
  再読み込みは起きない
- プロジェクトの一覧・グループ・並び順・名前を再起動をまたいで保持。最近使った順への
  並べ替えと、名前とパスによる絞り込み（`⌘⌃F`）
- `~/.ssh/config` のホストからリモートのワークスペースを開く。セッションの復元では
  リモートを自動で開かず、行を押したときに繋ぐ
- 隠れているプロジェクトのターミナルが処理を終えると、その行に印が付く。出力が
  流れている間は点が脈を打つ
- ストリップの配色はエディタのテーマに追従。ライト / ダークの固定も可
- 更新通知。`bundleCode.update.markerUrl` を設定したビルドだけで動き、ダウンロード・
  署名の検証・入れ替え・再起動まで行う。未保存の変更があれば確認が出る
- 拡張機能は Open VSX から取得。アイコンは自作。インストーラ・パッケージ情報・
  ヘルプのリンクは Microsoft から切り離した
